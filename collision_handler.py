class collision_handler(object):
    def __init__(self):
        pass

    def calculate_collisions(self, game_units_who_move):
        for unit in game_units_who_move:
            #find for each unit which moves if it has a collision
            #note, only solid items can hit each other
            #if it have, do proper action to units
            pass
